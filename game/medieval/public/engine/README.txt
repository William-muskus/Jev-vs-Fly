These scripts are copied from ../../web/engine by `npm run sync-engine`
(also run automatically as predev / prebuild). They are not git symlinks:
Windows clones without Developer Mode used to get a path-file instead of
JavaScript, and the hall then said "fly worker crashed".
